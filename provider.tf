provider "azurerm" {
    subscription_id = "{insert subscription ID}"
	tenant_id       = "{insert tenant ID}"
    client_id       = "{insert client or app ID}"
    client_secret   = "{insert client secret key}"    
}
